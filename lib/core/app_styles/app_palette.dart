import 'package:flutter/material.dart';

class AppPalette {
  static const Color primaryColor = Color.fromRGBO(42, 100, 219, 1);

  // Secondary Blue
  static const Color secondaryColor = Color.fromRGBO(123, 158, 243, 1);

  // Accent/Action colors
  static const Color successColor = Color.fromRGBO(76, 175, 80, 1);
  static const Color errorColor = Color.fromRGBO(244, 67, 54, 1);

  // Background
  static const Color background = Color.fromRGBO(248, 249, 252, 1);

  // Text colors
  static const Color textPrimary = Color.fromRGBO(33, 33, 33, 1);
  static const Color textSecondary = Color.fromRGBO(117, 117, 117, 1);

  // Neutral/UI colors
  static const Color neutral = Color.fromRGBO(224, 224, 224, 1);
  static const Color shadow = Color.fromRGBO(0, 0, 0, 0.1);
}
